name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: C Build
      run: make -f ./src/c/Makefile
    - name: Java Build
      run: make -f ./src/java/Makefile
    - name: Elm Build
      run: cd src/elm && make
